# Build artifacts — regenerate with qmk compile
*.hex
*.bin
*.elf
*.uf2
.build/

# Layer-card render (regenerate from make_card.py); keep if you'd rather version it
colemak_layer_card.pdf
original_alu40_zmk_card.pdf

# Python (layer-card generator)
__pycache__/
*.pyc

# OS cruft
.DS_Store
