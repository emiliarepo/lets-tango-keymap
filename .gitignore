# Build artifacts — regenerate with qmk compile
*.hex
*.bin
*.elf
*.uf2
.build/

# Layer-card renders (regenerate from make_card.py); keep if you'd rather version them
lets_tango_layer_card.pdf
macropad_card.pdf
original_alu40_zmk_card.pdf

# Python (layer-card generator)
__pycache__/
*.pyc

# OS cruft
.DS_Store
