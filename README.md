# Let's Tango — Colemak keymap

Personal QMK keymap for the **Let's Tango / Vitamins Included rev 2.1** — a split
4×12 ortholinear (integrated ATmega32U4, USB-C, 12× WS2812 underglow, speaker).

![build](https://github.com/emiliarepo/lets-tango-keymap/actions/workflows/build.yml/badge.svg)

## Layers

| #   | Layer      | What it is                                                       | Underglow |
| --- | ---------- | ---------------------------------------------------------------- | --------- |
| 0   | `_COLEMAK` | Base — Colemak                                                   | purple    |
| 1   | `_QWERTY`  | Base — QWERTY (toggle from Adjust)                               | green     |
| 2   | `_LOWER`   | Symbols on the left, NumLock keypad on the right                 | orange    |
| 3   | `_RAISE`   | Symbols / numbers                                                | cyan      |
| 4   | `_ADJUST`  | F1–F12, base toggles, underglow + audio/NKRO toggles, reset/boot | red       |

- **Base switch:** `PDF()` keys on Adjust persistently swap Colemak ↔ QWERTY
  (survives a power cycle).
- **Adjust** is reached by holding both thumb keys (Lower + Raise).
- A printable one-page reference is generated by `make_card.py`
  (`lets_tango_layer_card.pdf`).

## Features

- **Per-layer spinning underglow** — each layer keeps its own hue family and
  slowly rotates it around the 12-LED ring. The master computes the animation and
  syncs hue/phase to the slave over a split RPC so both halves stay in step. The
  colour follows the _physically held_ layer key, so it changes the instant you
  press.
- **Tap-dance layer lock** — double-tap Lower or Raise to lock that layer;
  double-tap again to unlock. Holding still works as a momentary layer.

## Repo layout

```
├── .github/workflows/build.yml   # CI: builds firmware + layer card
├── keymap.c                       # layers, tap dance, underglow, split sync
├── config.h                       # split RPC id
├── rules.mk                       # feature flags (tap dance)
├── make_card.py                   # renders the printable layer-reference card
├── README.md
└── .gitignore
```

## Build

Symlink this folder into a QMK checkout as the `colemak` keymap, then compile:

```bash
ln -s "$PWD" ~/qmk_firmware/keyboards/vitamins_included/keymaps/colemak
qmk compile -kb vitamins_included/rev2 -km colemak
```

Render the layer card locally:

```bash
pip install reportlab && python3 make_card.py   # -> lets_tango_layer_card.pdf
```

## CI

`.github/workflows/build.yml` runs on every push / PR (and on demand) and produces
two downloadable artifacts under **Actions → run → Artifacts**:

- `firmware-hex` — `vitamins_included_rev2_colemak.hex`
- `layer-card` — `lets_tango_layer_card.pdf`

## Flash

QMK Toolbox (macOS), **both halves**, same `.hex`. Bootloader is **qmk-dfu**; enter
it with the Adjust-layer `Boot` key or the reset pads. USB goes to the **left**
half (rev2 master = left); join the halves with the TRRS cable while unpowered.
Keep the OS on plain US QWERTY — Colemak lives in the firmware.
